Improved NVD CVE Project (with Identifier & Status)
--------------------------------------------------
Run:
  pip install -r requirements.txt
  python app.py
Open:
  http://127.0.0.1:5000/cves/list
Notes:
  - Click 'Sync (fetch 100)' to import a page from NVD.
  - The list shows Identifier and Status columns as requested.
